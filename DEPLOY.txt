MIND.EXE v4.9.0 FINAL QA

Upload the release while preserving the directory structure.
Do NOT flatten ai/, analytics/, config/, core/, features/, i18n/ or ui/ into the repository root.

Runtime files in this archive:
- index.html
- app.js
- splash.mp4
- splash-poster.jpg
- ai/
- analytics/
- config/
- core/
- features/
- i18n/
- ui/

Keep the existing repository PWA assets that are not part of this archive (manifest.json and icon PNG files).
The final index references manifest.json?v=4.9.0 and app.js?v=4.9.0-final.
All local JS module imports use ?v=4.9.0.

Recommended repository-only files:
- package.json
- tests/
- TESTING.md
- NEXT_CHAT_HANDOFF.md

Before deploy:
  npm test

Expected result:
  68 regression checks passed.
  MIND.EXE regression suite: OK

No data migration is required.
SCHEMA_VERSION remains 2.
Canonical profile/media/Strategy keys and Firestore path shape are unchanged.
Critical persistence stores are byte-for-byte unchanged from the tested v4.8.5.1 base.

# MIND.EXE v5.4.3

- Fixed Decision Lab condition textareas: spaces are preserved while typing; line normalization now happens only on blur/continue.
- Replaced the browser-native factor `<select>` with an in-app dark listbox matching MIND.EXE.
- Added bounded scrolling, selected-state checkmark, outside-click close and Escape handling for the factor picker.
- Regression suite expanded for both fixes.
